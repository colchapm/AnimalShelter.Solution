@{
  Layout = "_Layout";
}

@using AnimalShelter.Models;
@model List<AnimalShelter.Models.Animal>;

<h1>Animals</h1>

<ul>
  @foreach(Animal animal in Model)
  {
      <li>@Html.ActionLink($"{animal.Name}", "Details", new { id = animal.AnimalId })</li>
      @* <li>@Html.DisplayFor(model => animal.Breed) : @Html.DisplayFor(model => animal.Gender) : @Html.DisplayFor(model => animal.DateOfAdmittance) : @Html.DisplayFor(model => animal.Type) : @Html.DisplayFor(model => animal.AnimalId)</li> *@
  }
</ul>

<p>@Html.ActionLink("Add new animal", "Create")</p>